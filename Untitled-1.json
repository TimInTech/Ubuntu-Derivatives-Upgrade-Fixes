{
  "configurations": [
    {
      "type": "debugpy",
      "request": "launch",
      "name": "Launch Program",
      "program": "${workspaceFolder}/${input:programName}"
    }
  ],
  "inputs": [
    {
      "type": "promptString",
      "id": "programName",
      "description": "Enter the name of the Python file to debug"
    }
  ]
}